import { SkilldirectiveDirective } from './skilldirective.directive';

describe('SkilldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SkilldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
