import { JobdesignationDirective } from './jobdesignation.directive';

describe('JobdesignationDirective', () => {
  it('should create an instance', () => {
    const directive = new JobdesignationDirective();
    expect(directive).toBeTruthy();
  });
});
